# nixpacks.toml

[phases.setup]
nixPkgs = ["python310", "gcc", "ffmpeg"]
# KITA SUAPIN LIBRARY-NYA LANGSUNG:
# libgl1-mesa-glx: Ini obat paling ampuh buat error "libGL.so.1"
# libglib2.0-0: Buat jaga-jaga error GUI lainnya
aptPkgs = ["libgl1-mesa-glx", "libglib2.0-0"]

[phases.install]
cmds = [
  "python -m venv --copies /opt/venv",
  ". /opt/venv/bin/activate",
  "pip install --no-cache-dir --upgrade pip setuptools wheel",
  
  # Install Torch CPU (Wajib buat hemat RAM Free Plan)
  "pip install --no-cache-dir torch==2.0.1+cpu torchvision==0.15.2+cpu --index-url https://download.pytorch.org/whl/cpu",
  
  # Install sisanya
  "pip install --no-cache-dir -r requirements.txt",
  
  # Kita tetep coba uninstall driver NVIDIA biar gak berat-beratin image
  "pip uninstall -y nvidia-cudnn-cu12 nvidia-cublas-cu12 nvidia-cuda-runtime-cu12 ultralytics-thop 2>/dev/null || true",
  
  # Cleanup
  "find /opt/venv -type f -name '*.pyc' -delete"
]

[start]
cmd = "gunicorn --workers 1 --threads 2 --timeout 120 app:app"

[variables]
OPENCV_DISABLE_GUI = "1"
QT_QPA_PLATFORM = "offscreen"
